<%= form_for(@status2) do |f| %>
  <% if @status2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status2.errors.count, "error") %> prohibited this status2 from being saved:</h2>

      <ul>
      <% @status2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
